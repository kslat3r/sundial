apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: api
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
  - name: http
    protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP